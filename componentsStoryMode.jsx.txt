import { motion } from "framer-motion";
const slides = [
"Vaccination participation dropped by 22% in November.",
"Cold weather conditions increased transmission risk.",
"Two weeks later, infections surged rapidly.",
"Highly vaccinated regions remained stable."
];
export default function StoryMode() {
return (
<div className="min-h-screen flex flex-col justify-center items-center
bg-black text-white">
{slides.map((slide, index) => (
<motion.div
key={index}
initial={{ opacity: 0 }}
animate={{ opacity: 1 }}
transition={{ delay: index * 3 }}
className="text-4xl font-bold mb-10 text-center"
>
{slide}
</motion.div>
))}
</div>
);
}
